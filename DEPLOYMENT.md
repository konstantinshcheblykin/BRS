# Инструкция по развёртыванию

## 🚀 Быстрый старт (для непрофессионалов)

### Что нужно установить перед началом:

1. **Docker Desktop** - скачайте и установите с официального сайта: https://www.docker.com/products/docker-desktop/
2. **Git** (если проект клонируется из репозитория) - скачайте с: https://git-scm.com/downloads

### Развёртывание одной командой:

Откройте терминал (PowerShell или Command Prompt) в папке проекта и выполните:

```bash
docker compose up -d
```

**Вот и всё!** 🎉

Эта команда автоматически:
- ✅ Скачает и установит все необходимые компоненты (MySQL, PHP, Node.js)
- ✅ Создаст и настроит базу данных
- ✅ Установит все зависимости проекта
- ✅ Выполнит настройку приложения
- ✅ Запустит все сервисы

### ⏱️ Сколько это займёт?

При первом запуске процесс может занять **5-10 минут** (зависит от скорости интернета), так как Docker скачивает необходимые образы. При последующих запусках это займёт всего несколько секунд.

### ✅ Как проверить, что всё работает?

После выполнения команды подождите 1-2 минуты и откройте в браузере:

- **Frontend (веб-интерфейс)**: http://localhost:3000
- **Backend API**: http://localhost:8000/api/notes
- **Документация API**: http://localhost:8000/api/documentation

Если страницы открываются - всё работает! 🎊

### 📊 Проверка статуса

Чтобы посмотреть, запущены ли контейнеры:

```bash
docker compose ps
```

Все три сервиса (mysql, backend, frontend) должны быть в статусе "Up" или "running".

### 📝 Просмотр логов

Если что-то не работает, посмотрите логи:

```bash
# Логи всех сервисов
docker compose logs

# Логи конкретного сервиса
docker compose logs backend
docker compose logs frontend
docker compose logs mysql
```

---

## 🛑 Остановка приложения

Чтобы остановить приложение:

```bash
docker compose stop
```

Чтобы остановить и удалить контейнеры (данные в базе сохранятся):

```bash
docker compose down
```

Чтобы полностью удалить всё, включая данные базы данных:

```bash
docker compose down -v
```

---

## 🔄 Перезапуск

Если нужно перезапустить приложение:

```bash
docker compose restart
```

Или остановить и запустить заново:

```bash
docker compose down
docker compose up -d
```

---

## ❓ Решение проблем

### Проблема: Команда `docker compose` не найдена

**Решение:** Убедитесь, что Docker Desktop установлен и запущен. Проверьте, что Docker работает, выполнив:
```bash
docker --version
docker compose version
```

### Проблема: Порты заняты (ошибка "port is already allocated")

**Решение:** Порты 3000, 8000 или 3306 уже используются другим приложением. 

Варианты решения:
1. Остановите другие приложения, использующие эти порты
2. Или измените порты в файле `docker-compose.yml` (требуются базовые знания)

### Проблема: Контейнеры не запускаются

**Решение:**
1. Проверьте логи: `docker compose logs`
2. Убедитесь, что Docker Desktop запущен
3. Перезапустите Docker Desktop
4. Попробуйте пересобрать контейнеры: `docker compose up -d --build`

### Проблема: База данных не подключается

**Решение:** Подождите 30-60 секунд после запуска. MySQL нужно время для инициализации. Проверьте логи MySQL:
```bash
docker compose logs mysql
```

### Проблема: Frontend не открывается или показывает ошибку

**Решение:**
1. Убедитесь, что backend запущен: `docker compose ps`
2. Проверьте логи frontend: `docker compose logs frontend`
3. Подождите 1-2 минуты - frontend может долго компилироваться при первом запуске

### Проблема: Страница открывается, но API не работает

**Решение:**
1. Проверьте, что backend запущен: `docker compose ps`
2. Проверьте логи backend: `docker compose logs backend`
3. Попробуйте перезапустить backend: `docker compose restart backend`

---

## 🔧 Дополнительные команды (для продвинутых пользователей)

### Ручная настройка (если автоматическая не сработала)

Если автоматическая настройка не сработала, можно выполнить команды вручную:

```bash
# Установка зависимостей Laravel
docker compose exec backend composer install

# Генерация ключа приложения
docker compose exec backend php artisan key:generate

# Выполнение миграций базы данных
docker compose exec backend php artisan migrate

# Генерация Swagger документации
docker compose exec backend php artisan l5-swagger:generate
```

### Обновление кода

После обновления кода из репозитория:

```bash
# Перезапуск контейнеров
docker compose restart

# Или полная пересборка (если изменились зависимости)
docker compose up -d --build
```

### Тестирование

```bash
# Запуск всех тестов
docker compose exec backend php artisan test

# Запуск конкретного теста
docker compose exec backend php artisan test --filter NoteApiTest
```

### Резервное копирование базы данных

```bash
# Экспорт базы данных
docker compose exec mysql mysqldump -u notes_user -pnotes_password notes_db > backup.sql

# Импорт базы данных
docker compose exec -T mysql mysql -u notes_user -pnotes_password notes_db < backup.sql
```

---

## 📚 Полезные ссылки

- [Документация Docker](https://docs.docker.com/)
- [Документация Docker Compose](https://docs.docker.com/compose/)
- [Laravel документация](https://laravel.com/docs)
- [React документация](https://react.dev/)

---

## 💡 Советы

1. **Первый запуск:** При первом запуске наберитесь терпения - Docker скачивает образы (может занять 5-10 минут)

2. **Проверка работы:** Всегда проверяйте статус командой `docker compose ps` перед тем, как обращаться за помощью

3. **Логи:** Если что-то не работает, сначала проверьте логи - там обычно написано, в чём проблема

4. **Перезапуск:** Часто помогает простой перезапуск: `docker compose restart`

5. **Очистка:** Если что-то совсем не работает, попробуйте полную очистку и пересборку:
   ```bash
   docker compose down -v
   docker compose up -d --build
   ```
