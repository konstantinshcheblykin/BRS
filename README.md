# Notes Service - Веб-сервис для управления заметками

Веб-сервис для создания, просмотра, редактирования и удаления заметок с RESTful API и React UI.

## Архитектура и выбранные технологии:

**Backend:**

- **Laravel 10** - современный PHP фреймворк с отличной поддержкой RESTful API, встроенной валидацией и ORM
- **MySQL 8** - надежная реляционная БД
- **PHPUnit** - стандартный фреймворк для тестирования PHP приложений

**Frontend:**
- **React 18** - популярный и гибкий фреймворк для создания UI
- **Axios** - для HTTP запросов к API

**Инфраструктура:**

- **Docker & Docker Compose** - контейнеризация для простого развёртывания
- **Nginx** - веб-сервер для фронтенда

**Документация:**
- **Swagger/OpenAPI** - через L5-Swagger для Laravel

### Преимущества архитектуры:

1. **Laravel** - быстрая разработка, встроенная валидация, миграции БД, удобная работа с API
2. **React** - компонентный подход, быстрый UI, хорошая экосистема
3. **Docker** - изолированная среда, простое развёртывание, одинаковое окружение для всех
4. **MySQL** - надежность, производительность, широкое распространение

## Структура проекта

```text
/
├── backend/              # Laravel приложение
│   ├── app/
│   │   ├── Http/Controllers/
│   │   ├── Models/
│   │   └── Requests/
│   ├── database/migrations/
│   ├── tests/
│   └── ...
├── frontend/            # React приложение
│   ├── src/
│   │   ├── components/
│   │   ├── services/
│   │   └── App.jsx
│   └── ...
├── docker-compose.yml
└── README.md
```

## 🚀 Быстрый старт

### Требования:

- **Docker Desktop** - скачайте и установите с https://www.docker.com/products/docker-desktop/
- **Git** (опционально, только если клонируете из репозитория)

### Развёртывание одной командой:

#### Вариант 1: Для Windows (самый простой способ)

Просто дважды кликните на файл **`start.bat`** в корне проекта.

Или откройте терминал в папке проекта и выполните:

```bash
start.bat
```

#### Вариант 2: Через командную строку

Откройте терминал (PowerShell или Command Prompt) в папке проекта и выполните:

```bash
docker compose up -d
```


### ⏱️ Время запуска

При первом запуске процесс может занять **5-10 минут** (Docker скачивает необходимые образы). При последующих запусках - всего несколько секунд.

### ✅ Проверка работы

Подождите 1-2 минуты после запуска и откройте в браузере:

- **Frontend (веб-интерфейс)**: http://localhost:3000 (веб-интерфейс)**: http://localhost:3000
- **Backend API**: http://localhost:8000/api/notes
- **API Documentation (Swagger)**: http://localhost:8000/api/documentation

Если страницы открываются - всё работает! 🎊

### 📋 Что происходит автоматически?

При первом запуске Docker автоматически:

- ✅ Скачает и установит все необходимые компоненты (MySQL, PHP, Node.js)
- ✅ Установит все зависимости проекта (Composer для Laravel, npm для React)
- ✅ Создаст и настроит базу данных
- ✅ Выполнит миграции базы данных
- ✅ Сгенерирует Swagger документацию
- ✅ Запустит все сервисы

### 📖 Подробная инструкция

Для более подробной информации и решения проблем см. [DEPLOYMENT.md](DEPLOYMENT.md)

## API Endpoints

### Базовый URL: `http://localhost:8000/api`

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/notes` | Получить все заметки |
| GET | `/notes/{id}` | Получить заметку по ID |
| POST | `/notes` | Создать новую заметку |
| PUT | `/notes/{id}` | Обновить заметку |
| DELETE | `/notes/{id}` | Удалить заметку |

### Примеры запросов:

**Создание заметки:**

```bash
curl -X POST http://localhost:8000/api/notes \
  -H "Content-Type: application/json" \
  -d '{"title": "Моя заметка", "content": "Содержание заметки"}'
```

**Получение всех заметок:**
```bash
curl http://localhost:8000/api/notes
```

**Обновление заметки:**

```bash
curl -X PUT http://localhost:8000/api/notes/1 \
  -H "Content-Type: application/json" \
  -d '{"title": "Обновленная заметка", "content": "Новое содержание"}'
```

**Удаление заметки:**
```bash
curl -X DELETE http://localhost:8000/api/notes/1
```

## Валидация данных

### Создание/Обновление заметки:

- `title` (обязательно, строка, минимум 1 символ, максимум 255 символов)
- `content` (обязательно, строка, минимум 1 символ)

## Тестирование

### Расположение тестов:

API тесты находятся в файле: `backend/tests/Feature/NoteApiTest.php`

### Запуск PHPUnit тестов:

```bash
# Запустить все тесты
docker compose exec backend php artisan test

# Или через PHPUnit напрямую
docker compose exec backend vendor/bin/phpunit

# Запустить только тесты для Note API
docker compose exec backend php artisan test --filter NoteApiTest

# Запустить конкретный тест
docker compose exec backend php artisan test --filter test_can_create_note
```

### Структура тестов:

Тесты покрывают все 5 API методов и включают:

- ✅ Получение всех заметок (`test_can_get_all_notes`)
- ✅ Получение одной заметки (`test_can_get_single_note`)
- ✅ Создание заметки (`test_can_create_note`)
- ✅ Обновление заметки (`test_can_update_note`)
- ✅ Удаление заметки (`test_can_delete_note`)
- ✅ Валидация данных (отсутствие title/content)
- ✅ Обработка ошибок (404 для несуществующих заметок)

**Подробная документация по тестированию:** см. [TESTING.md](TESTING.md)

## Документация API

Swagger документация доступна по адресу:

- http://localhost:8000/api/documentation

## 🛑 Остановка приложения

```bash
# Остановка без удаления данных
docker compose stop

# Остановка с удалением контейнеров (данные сохранятся)
docker compose down

# Полная очистка (включая данные базы данных)
docker compose down -v
```

**Для Windows:** Можно также использовать файл `stop.bat` (если создан) или просто закрыть окно терминала и выполнить команду выше.

## Разработка

### Backend (Laravel):

```bash
# Войти в контейнер
docker compose exec backend bash

# Выполнить команды Laravel
docker compose exec backend php artisan [command]

# Просмотр логов
docker compose logs -f backend
```

### Frontend (React):

```bash
# Войти в контейнер
docker compose exec frontend sh

# Просмотр логов
docker compose logs -f frontend
```

### Полезные команды:

```bash
# Перезапустить все сервисы
docker compose restart

# Остановить все сервисы
docker compose stop

# Просмотр статуса
docker compose ps

# Просмотр логов всех сервисов
docker compose logs -f
```

## Структура базы данных

Таблица `notes`:

- `id` - первичный ключ (auto increment)
- `title` - заголовок заметки (VARCHAR 255)
- `content` - содержание заметки (TEXT)
- `created_at` - дата создания
- `updated_at` - дата обновления


